extends layout

block content
  .container
    table.table.table-striped
      thead.thead-inverse
        tr
          th Name
          th Quantity
      tbody
        - recommends.forEach((recommend)=>{
          tr
            td.cell
              a(href=recommend.url target="_blank") 
                strong #{recommend.name}
            td.cell #{recommend.count}
        - })

